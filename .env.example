# Database connection URL (Firebase Project ID or Firestore URL)
DATABASE_URL="gen-lang-client-0759694858"
FIREBASE_PROJECT_ID="gen-lang-client-0759694858"

# JWT secret for authentication (Firebase Auth handles this, but keeping for legacy)
JWT_SECRET="firebase-managed"

# Paystack API keys
PAYSTACK_SECRET_KEY="your_paystack_secret_key"
PAYSTACK_WEBHOOK_SECRET="your_paystack_webhook_secret"

# Server configuration
PORT=3000
NODE_ENV="development"
